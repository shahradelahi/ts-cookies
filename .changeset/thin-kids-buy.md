---
'@se-oss/cookies': patch
---

Correctly include `.d.cts` type declaration files and configure explicit `import`/`require` type conditions in `package.json` to fix TypeScript module resolution under Node16/NodeNext.
